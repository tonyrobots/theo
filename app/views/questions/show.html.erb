
<h1>  <%= sanitize(@question.body) %> </h1>
<div id="subline">
	by <%= getUsernameLinkById(@question.user_id)%>, <%= time_ago_in_words(@question.created_at) %> ago

<p>
<%= render ("/shared/comment_form")%>
</p>

<%= 
if current_user_is?(@question.user_id)
	link_to 'Edit', edit_question_path(@question) 
end %>
<%= link_to 'Back', questions_path %>
